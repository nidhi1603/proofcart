# Pin loosely; re-check exact versions at build time (`pip index versions <pkg>`).
anthropic>=0.40          # buyer/extraction agent + optional LLM judge
pydantic>=2.7,<3         # typed contracts
pyyaml>=6.0              # scenarios + mandate config
stripe>=10              # settlement (TEST MODE ONLY)
slack_sdk>=3.27          # read negotiations, post shortlist/outcome, verify owner identity
notion-client>=2.2       # durable comparison/decision record
python-dotenv>=1.0       # load .env
pytest>=8                # tests
