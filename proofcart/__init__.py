"""ProofCart — an owner-controlled purchasing assistant.

Collect supplier negotiations from Slack -> reconstruct each offer ->
compare and rank with evidence quality -> present eligible options ->
the owner approves the exact quote -> execute one crash-safe payment ->
record and verify across Notion + Slack.
"""

__version__ = "0.1.0"
